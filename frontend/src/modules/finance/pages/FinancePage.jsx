import PageHeader from '@components/common/PageHeader';

/**
 * FinancePage
 * -----------------------------------------------------------------------
 * Placeholder page for the "finance" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out finance screens here, sourcing data via
 * `services/finance.service.js` (never fetch directly from a component).
 */
function FinancePage() {
  return <PageHeader title="Finance" subtitle="This module is scaffolded and ready to build." />;
}

export default FinancePage;
