import PageHeader from '@components/common/PageHeader';

/**
 * OperationsPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "operations" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out operations screens here, sourcing data via
 * `services/operations.service.js` (never fetch directly from a component).
 */
function OperationsPage() {
  return <PageHeader title="Operations" subtitle="This module is scaffolded and ready to build." />;
}

export default OperationsPage;
