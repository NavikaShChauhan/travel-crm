import { useState } from 'react';
import { Box, Button, Card, Chip, Drawer, IconButton, LinearProgress, Stack, Typography } from '@mui/material';
import { MdClose, MdOutlineCheckCircle } from 'react-icons/md';
import { tokens } from '@styles/theme';

const BOOKINGS = [
  { id: 'CNF-501', customer: 'Vikram & Nisha Patel', proposal: 'PR-2605', destination: 'Switzerland', executive: 'Priya Sharma', payment: 'Pending', documents: '1 of 2', confirmation: 'Soft Confirm', progress: 42 },
  { id: 'CNF-502', customer: 'Aarav Singh', proposal: 'PR-2591', destination: 'Dubai', executive: 'Meera Pillai', payment: 'Received', documents: '2 of 2', confirmation: 'Confirmed', progress: 85 },
  { id: 'CNF-503', customer: 'Neha Kulkarni', proposal: 'PR-2587', destination: 'Vietnam', executive: 'Arjun Nair', payment: 'Pending', documents: '0 of 2', confirmation: 'Payment Pending', progress: 25 },
];
function ConfirmationWorkspace() { const [selected, setSelected] = useState(null); const metrics = [['Soft Confirm', 1], ['Confirmed', 1], ['Payment Pending', 2], ['Awaiting Documents', 2], ['Ready for Operations', 1], ['Handed Over', 0]]; return <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2.5 }}><Box><Typography variant="h5" sx={{ fontWeight: 700 }}>Confirmation</Typography><Typography variant="body2" color="text.secondary">Finalize customer approvals, collect confirmations, and hand over confirmed bookings to Operations.</Typography></Box><Box sx={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(155px, 1fr))', gap: 1.25 }}>{metrics.map(([label, value], index) => <Card key={label} sx={{ p: 1.75, borderTop: `3px solid ${index > 3 ? tokens.color.teal500 : tokens.color.navy700}` }}><Typography variant="caption" color="text.secondary">{label}</Typography><Typography variant="h6" sx={{ fontWeight: 700, mt: 0.5 }}>{value}</Typography></Card>)}</Box><Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr', lg: '1.55fr 1fr' }, gap: 2.5 }}><Card sx={{ p: { xs: 2, md: 2.5 } }}><Typography variant="subtitle1" sx={{ fontWeight: 700 }}>Booking Readiness</Typography><Typography variant="body2" color="text.secondary" sx={{ mb: 2 }}>Complete the requirements before handing a booking to Operations.</Typography><Stack spacing={1.25}>{BOOKINGS.map((booking) => <Box key={booking.id} onClick={() => setSelected(booking)} sx={{ p: 1.5, border: '1px solid', borderColor: 'divider', borderRadius: 2, cursor: 'pointer', '&:hover': { bgcolor: 'background.default' } }}><Stack direction="row" justifyContent="space-between" spacing={1}><Box><Typography variant="body2" sx={{ fontWeight: 700 }}>{booking.customer}</Typography><Typography variant="caption" color="text.secondary">{booking.proposal} · {booking.destination} · {booking.executive}</Typography></Box><Chip label={booking.confirmation} size="small" color={booking.confirmation === 'Confirmed' ? 'success' : 'default'} /></Stack><Stack direction="row" justifyContent="space-between" sx={{ mt: 1 }}><Typography variant="caption">Payment: {booking.payment}</Typography><Typography variant="caption">Documents: {booking.documents}</Typography></Stack><LinearProgress variant="determinate" value={booking.progress} sx={{ mt: 1, height: 6, borderRadius: 4 }} /></Box>)}</Stack></Card><Card sx={{ p: { xs: 2, md: 2.5 } }}><Typography variant="subtitle1" sx={{ fontWeight: 700 }}>Handover Checklist</Typography><Stack spacing={1.5} sx={{ mt: 2 }}>{['Customer confirmation', 'Payment received', 'Documents verified', 'Booking reference created', 'Operations handover'].map((item, index) => <Stack key={item} direction="row" spacing={1} alignItems="center"><MdOutlineCheckCircle size={18} color={index < 2 ? tokens.color.teal500 : tokens.color.ink400} /><Typography variant="body2" color={index < 2 ? 'text.primary' : 'text.secondary'}>{item}</Typography></Stack>)}</Stack><Button variant="contained" fullWidth sx={{ mt: 3 }}>Transfer Ready Bookings</Button></Card></Box><Drawer anchor="right" open={Boolean(selected)} onClose={() => setSelected(null)} PaperProps={{ sx: { width: { xs: '100%', sm: 'min(42vw, 560px)' }, minWidth: { sm: 400 }, maxWidth: '100%' } }}>{selected && <Box sx={{ p: 3 }}><Stack direction="row" justifyContent="space-between"><Box><Typography variant="h6" sx={{ fontWeight: 700 }}>{selected.customer}</Typography><Typography variant="body2" color="text.secondary">{selected.proposal} · {selected.destination}</Typography></Box><IconButton onClick={() => setSelected(null)}><MdClose /></IconButton></Stack><Stack spacing={2} sx={{ mt: 3 }}><Typography variant="subtitle2">Booking requirements</Typography>{[['Payment', selected.payment], ['Documents', selected.documents], ['Confirmation', selected.confirmation], ['Executive', selected.executive]].map(([label, value]) => <Stack key={label} direction="row" justifyContent="space-between"><Typography variant="body2" color="text.secondary">{label}</Typography><Typography variant="body2" sx={{ fontWeight: 700 }}>{value}</Typography></Stack>)}<Button variant="outlined">Record Payment</Button><Button variant="outlined">Upload Documents</Button><Button variant="outlined">Send Confirmation Email</Button><Button variant="contained">Transfer to Operations</Button></Stack></Box>}</Drawer></Box>; }
export default ConfirmationWorkspace;
