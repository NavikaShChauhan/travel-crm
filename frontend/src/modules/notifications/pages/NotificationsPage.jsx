import PageHeader from '@components/common/PageHeader';

/**
 * NotificationsPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "notifications" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out notifications screens here, sourcing data via
 * `services/notifications.service.js` (never fetch directly from a component).
 */
function NotificationsPage() {
  return <PageHeader title="Notifications" subtitle="This module is scaffolded and ready to build." />;
}

export default NotificationsPage;
