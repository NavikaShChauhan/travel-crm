import PageHeader from '@components/common/PageHeader';

/**
 * SuppliersPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "suppliers" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out suppliers screens here, sourcing data via
 * `services/suppliers.service.js` (never fetch directly from a component).
 */
function SuppliersPage() {
  return <PageHeader title="Supplier Management" subtitle="This module is scaffolded and ready to build." />;
}

export default SuppliersPage;
