import PageHeader from '@components/common/PageHeader';

/**
 * SettingsPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "settings" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out settings screens here, sourcing data via
 * `services/settings.service.js` (never fetch directly from a component).
 */
function SettingsPage() {
  return <PageHeader title="Settings" subtitle="This module is scaffolded and ready to build." />;
}

export default SettingsPage;
