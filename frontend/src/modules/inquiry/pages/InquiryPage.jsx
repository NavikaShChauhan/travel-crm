import PageHeader from '@components/common/PageHeader';

/**
 * InquiryPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "inquiry" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out inquiry screens here, sourcing data via
 * `services/inquiry.service.js` (never fetch directly from a component).
 */
function InquiryPage() {
  return <PageHeader title="Inquiry Engine" subtitle="This module is scaffolded and ready to build." />;
}

export default InquiryPage;
