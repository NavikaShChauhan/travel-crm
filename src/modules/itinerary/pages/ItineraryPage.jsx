import PageHeader from '@components/common/PageHeader';

/**
 * ItineraryPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "itinerary" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out itinerary screens here, sourcing data via
 * `services/itinerary.service.js` (never fetch directly from a component).
 */
function ItineraryPage() {
  return <PageHeader title="Itinerary Builder" subtitle="This module is scaffolded and ready to build." />;
}

export default ItineraryPage;
