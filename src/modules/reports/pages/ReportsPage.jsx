import PageHeader from '@components/common/PageHeader';

/**
 * ReportsPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "reports" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out reports screens here, sourcing data via
 * `services/reports.service.js` (never fetch directly from a component).
 */
function ReportsPage() {
  return <PageHeader title="Reports" subtitle="This module is scaffolded and ready to build." />;
}

export default ReportsPage;
