import { useState } from 'react';
import { Box, Button, Card, Chip, Divider, Stack, Typography } from '@mui/material';
import { MdCall, MdEmail, MdOutlineCheckCircle, MdOutlineSchedule, MdWhatsapp } from 'react-icons/md';
import { tokens } from '@styles/theme';

const TASKS = [
  { time: '10:30', customer: 'The Khanna Family', destination: 'Rajasthan', mode: 'Call', status: 'Priority', reason: 'Discuss hotel upgrade and final price', response: 'Requested changes' },
  { time: '11:00', customer: 'Rohan & Anjali Mehta', destination: 'Bali', mode: 'WhatsApp', status: 'Today', reason: 'Follow up on revised price', response: 'Thinking' },
  { time: '14:30', customer: 'Siddharth Verma', destination: 'Kerala', mode: 'Call', status: 'Today', reason: 'Understand package preferences', response: 'Viewed proposal' },
  { time: '16:00', customer: 'Deepika Reddy', destination: 'Andaman', mode: 'Email', status: 'Upcoming', reason: 'Share flight options', response: 'Waiting for approval' },
];
function FollowUpDashboard() { const [selected, setSelected] = useState(TASKS[0]); const [completed, setCompleted] = useState([]); const metrics = [['Calls today', 3], ['Waiting for response', 4], ['Overdue', 1], ['Completed today', completed.length], ['Best time to call', '10–12 pm'], ['Conversion this week', '22%']]; return <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2.5 }}><Box><Typography variant="h5" sx={{ fontWeight: 700 }}>Follow Up Planner</Typography><Typography variant="body2" color="text.secondary">A calm, day-first view of the conversations that move proposals forward.</Typography></Box><Box sx={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(150px, 1fr))', gap: 1.25 }}>{metrics.map(([label, value], index) => <Card key={label} sx={{ p: 1.5, borderTop: `3px solid ${index === 2 ? tokens.color.coral500 : index === 5 ? tokens.color.teal500 : tokens.color.navy700}` }}><Typography variant="caption" color="text.secondary">{label}</Typography><Typography variant="h6" sx={{ fontWeight: 700, mt: 0.5 }}>{value}</Typography></Card>)}</Box><Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr', lg: '1.25fr .9fr' }, gap: 2.5 }}><Card sx={{ p: { xs: 2, md: 2.5 } }}><Stack direction="row" justifyContent="space-between" alignItems="center"><Box><Typography variant="subtitle1" sx={{ fontWeight: 700 }}>Today’s conversation plan</Typography><Typography variant="body2" color="text.secondary">Tap a customer to prepare for the next touchpoint.</Typography></Box><Chip label="03 Aug" color="primary" /></Stack><Stack spacing={0} divider={<Divider flexItem />} sx={{ mt: 2 }}>{TASKS.map((task) => <Stack key={task.customer} direction="row" spacing={1.5} onClick={() => setSelected(task)} sx={{ py: 1.5, cursor: 'pointer', opacity: completed.includes(task.customer) ? .5 : 1 }}><Typography variant="subtitle2" sx={{ width: 42, color: tokens.color.navy700 }}>{task.time}</Typography><Box sx={{ width: 3, borderRadius: 3, bgcolor: task.status === 'Priority' ? tokens.color.coral500 : tokens.color.navy700 }} /><Box sx={{ flex: 1 }}><Stack direction="row" justifyContent="space-between" gap={1}><Typography variant="body2" sx={{ fontWeight: 700 }}>{task.customer}</Typography><Chip size="small" label={task.mode} variant="outlined" /></Stack><Typography variant="caption" color="text.secondary">{task.destination} · {task.reason}</Typography></Box></Stack>)}</Stack></Card><Card sx={{ p: { xs: 2, md: 2.5 }, bgcolor: 'rgba(49,68,110,.04)' }}><Typography variant="subtitle1" sx={{ fontWeight: 700 }}>Prepare for this conversation</Typography><Typography variant="h6" sx={{ mt: 2, fontWeight: 700 }}>{selected.customer}</Typography><Typography variant="body2" color="text.secondary">{selected.destination} · {selected.mode}</Typography><Box sx={{ p: 1.5, mt: 2, borderRadius: 2, bgcolor: 'background.paper' }}><Typography variant="caption" color="text.secondary">Customer signal</Typography><Typography variant="body2" sx={{ fontWeight: 700 }}>{selected.response}</Typography></Box><Box sx={{ p: 1.5, mt: 1, borderRadius: 2, bgcolor: 'background.paper' }}><Typography variant="caption" color="text.secondary">Conversation goal</Typography><Typography variant="body2" sx={{ fontWeight: 700 }}>{selected.reason}</Typography></Box><Stack direction="row" gap={1} flexWrap="wrap" sx={{ mt: 2.5 }}><Button size="small" variant="contained" startIcon={<MdCall />}>Start call</Button><Button size="small" variant="outlined" startIcon={<MdWhatsapp />}>WhatsApp</Button><Button size="small" variant="outlined" startIcon={<MdEmail />}>Email</Button><Button size="small" variant="outlined" startIcon={<MdOutlineSchedule />}>Reschedule</Button><Button size="small" startIcon={<MdOutlineCheckCircle />} onClick={() => setCompleted((items) => [...items, selected.customer])}>Complete</Button></Stack></Card></Box></Box>; }
export default FollowUpDashboard;
