import PageHeader from '@components/common/PageHeader';

/**
 * SalesPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "sales" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out sales screens here, sourcing data via
 * `services/sales.service.js` (never fetch directly from a component).
 */
function SalesPage() {
  return <PageHeader title="Sales Engine" subtitle="This module is scaffolded and ready to build." />;
}

export default SalesPage;
