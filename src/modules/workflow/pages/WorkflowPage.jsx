import PageHeader from '@components/common/PageHeader';

/**
 * WorkflowPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "workflow" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out workflow screens here, sourcing data via
 * `services/workflow.service.js` (never fetch directly from a component).
 */
function WorkflowPage() {
  return <PageHeader title="Workflow Automation" subtitle="This module is scaffolded and ready to build." />;
}

export default WorkflowPage;
