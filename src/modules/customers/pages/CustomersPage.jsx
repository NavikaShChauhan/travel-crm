import PageHeader from '@components/common/PageHeader';

/**
 * CustomersPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "customers" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out customers screens here, sourcing data via
 * `services/customers.service.js` (never fetch directly from a component).
 */
function CustomersPage() {
  return <PageHeader title="Customer Management" subtitle="This module is scaffolded and ready to build." />;
}

export default CustomersPage;
