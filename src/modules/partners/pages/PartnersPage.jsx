import PageHeader from '@components/common/PageHeader';

/**
 * PartnersPage
 * -----------------------------------------------------------------------
 * Placeholder page for the "partners" module. This is intentionally bare —
 * the module's own pages/components/services/data folders are scaffolded
 * and ready, but real screens are built module-by-module later.
 *
 * TODO: build out partners screens here, sourcing data via
 * `services/partners.service.js` (never fetch directly from a component).
 */
function PartnersPage() {
  return <PageHeader title="Partner Management" subtitle="This module is scaffolded and ready to build." />;
}

export default PartnersPage;
